apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: dapr-jobs
spec:
  type: bindings.cron
  version: v1
  metadata:
  - name: schedule
    value: "0 * * * *"  # Default: every hour (can be overridden per job)
