apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: recurring-tasks
spec:
  type: bindings.cron
  version: v1
  metadata:
  - name: schedule
    value: "*/5 * * * *"  # Every 5 minutes - checks for recurring task patterns
  - name: direction
    value: "input"
scopes:
- todo-backend
