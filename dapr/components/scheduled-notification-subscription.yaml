apiVersion: dapr.io/v1alpha1
kind: Subscription
metadata:
  name: scheduled-notification-subscription
spec:
  topic: notifications
  route: /dapr/notifications/scheduled
  pubsubname: notification-pubsub
scopes:
- todo-backend
